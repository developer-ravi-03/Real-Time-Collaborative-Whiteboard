"use client";
import { motion } from "framer-motion";
import { ArrowUpRight, Check, Sparkles } from "lucide-react";
export function AiFeatures() { return <section className="bg-foreground py-24 text-background sm:py-32"><div className="mx-auto grid max-w-7xl items-center gap-14 px-5 lg:grid-cols-2 lg:px-8"><div><p className="text-xs font-bold tracking-[.18em] text-indigo-300 uppercase">SyncBoard intelligence</p><h2 className="mt-5 text-4xl font-semibold tracking-[-.045em] sm:text-5xl">AI that understands the work—not just the words.</h2><p className="mt-6 max-w-xl text-lg leading-8 text-background/65">Your project’s goals, decisions, and delivery signals become useful context. That’s how SyncBoard turns a status update into a clear next move.</p><div className="mt-8 space-y-4">{["Turn conversations into decisions and owners","Spot risk before it becomes a surprise","Create briefings tailored to every stakeholder"].map(item => <p key={item} className="flex items-center gap-3 text-sm"><span className="grid size-5 place-items-center rounded-full bg-indigo-400 text-indigo-950"><Check className="size-3.5" /></span>{item}</p>)}</div><a href="#pricing" className="mt-9 inline-flex items-center gap-2 text-sm font-semibold text-indigo-300 hover:text-indigo-200">Meet your project copilot <ArrowUpRight className="size-4" /></a></div><motion.div initial={{ opacity: 0, scale: .96 }} whileInView={{ opacity: 1, scale: 1 }} viewport={{ once: true }} className="rounded-3xl border border-white/10 bg-white/[.07] p-4 shadow-2xl"><div className="rounded-2xl bg-[#15151d] p-5"><div className="flex items-center gap-3 border-b border-white/10 pb-4"><span className="grid size-9 place-items-center rounded-xl bg-indigo-400/15 text-indigo-300"><Sparkles className="size-4" /></span><div><p className="text-sm font-semibold">Project brief</p><p className="text-xs text-white/45">Generated from this week’s work</p></div></div><p className="mt-5 text-sm leading-6 text-white/70">The launch is on track, with the narrative and dashboard both moving into review. The only dependency at risk is the final billing handoff.</p><div className="mt-5 space-y-2">{["Confirm billing scope with engineering", "Schedule narrative review", "Share launch readiness summary"].map((task, i) => <div key={task} className="flex items-center gap-3 rounded-xl bg-white/[.06] p-3 text-xs"><span className="grid size-5 place-items-center rounded-md bg-indigo-400 text-[10px] font-bold text-indigo-950">{i + 1}</span>{task}</div>)}</div></div></motion.div></div></section>; }
