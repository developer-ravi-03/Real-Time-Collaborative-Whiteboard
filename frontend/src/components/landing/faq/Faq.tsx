"use client";
import { useState } from "react";
import { ChevronDown } from "lucide-react";
const questions = [["Can I use SyncBoard for free?","Yes. The Starter plan is free for teams of any size and includes the core workspace, three active projects, and real-time collaboration."],["What does the AI assistant have access to?","It works only with the work your team has permission to see. Workspace controls and privacy settings apply to all AI-powered features."],["Can we move from another project tool?","Absolutely. Our import tools make it straightforward to bring over projects, tasks, and essential context from the tools you already use."],["Is SyncBoard suitable for client collaboration?","Yes. Invite guests to the specific spaces they need, with clear controls over what they can view, comment on, and edit."]];
export function Faq() { const [open, setOpen] = useState(0); return <section className="py-24 sm:py-32"><div className="mx-auto max-w-3xl px-5"><div className="text-center"><p className="text-xs font-bold tracking-[.18em] text-indigo-600 uppercase dark:text-indigo-400">Questions, answered</p><h2 className="mt-4 text-3xl font-semibold tracking-[-.04em] sm:text-4xl">Everything you need to know.</h2></div><div className="mt-12 divide-y divide-border border-y">{questions.map(([question,answer], i) => <div key={question}><button className="flex w-full items-center justify-between gap-5 py-5 text-left text-base font-semibold" aria-expanded={open === i} onClick={() => setOpen(open === i ? -1 : i)}>{question}<ChevronDown className={`size-5 shrink-0 text-muted-foreground transition-transform ${open === i ? "rotate-180" : ""}`} /></button>{open === i && <p className="-mt-1 pb-5 pr-10 text-sm leading-6 text-muted-foreground">{answer}</p>}</div>)}</div></div></section>; }
